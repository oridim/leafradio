export const FILE_HASH_WORKER = new URL(
    './hash-worker.ts',
    import.meta.url,
);

export const FILE_MUSICAL_FEATURES_WORKER = new URL(
    './musical-features-worker.ts',
    import.meta.url,
);
