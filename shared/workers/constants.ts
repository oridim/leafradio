export const FILE_AUDIO_PROCESSING_WORKER = new URL(
    './audio-processing-worker.ts',
    import.meta.url,
);

export const FILE_HASH_WORKER = new URL(
    './hash-worker.ts',
    import.meta.url,
);
